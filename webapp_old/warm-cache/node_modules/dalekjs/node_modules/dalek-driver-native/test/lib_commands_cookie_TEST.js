'use strict';

var expect = require('chai').expect;
var Cookie = require('../lib/commands/cookie.js');

describe('dalek-driver-native Commands Cookie', function() {

  it('i should think of proper testing', function(){
    expect(Cookie).to.be.ok;
  });

});
