'use strict';

var expect = require('chai').expect;
var Cookie = require('../lib/commands/cookie.js');

describe('dalek-internal-webdriver Command/Cookie', function () {

  it('should exist', function () {
    expect(Cookie).to.be.ok;
  });

});
